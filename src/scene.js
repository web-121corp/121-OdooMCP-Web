import * as THREE from 'three';
import { gsap } from 'gsap';
export function initScene(canvas){if(!canvas)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(42,canvas.clientWidth/canvas.clientHeight,.1,100),renderer=new THREE.WebGLRenderer({canvas,antialias:true,alpha:true});camera.position.z=6.5;renderer.setPixelRatio(Math.min(devicePixelRatio,2));const group=new THREE.Group();scene.add(group);const geo=new THREE.IcosahedronGeometry(1.72,2);group.add(new THREE.Mesh(geo,new THREE.MeshBasicMaterial({color:0x2447ff,wireframe:true,transparent:true,opacity:.72})));group.add(new THREE.Mesh(new THREE.IcosahedronGeometry(1.22,1),new THREE.MeshBasicMaterial({color:0x8a9aff,wireframe:true,transparent:true,opacity:.26})));group.add(new THREE.Points(geo,new THREE.PointsMaterial({color:0xffffff,size:.035,opacity:.9,transparent:true})));for(let i=0;i<90;i++){const r=2.3+Math.random()*1.5,a=Math.random()*Math.PI*2,y=(Math.random()-.5)*3.5;const m=new THREE.Mesh(new THREE.SphereGeometry(.018,4,4),new THREE.MeshBasicMaterial({color:0x2447ff}));m.position.set(Math.cos(a)*r,y,Math.sin(a)*r);scene.add(m)}gsap.to(group.rotation,{y:Math.PI*2,duration:22,repeat:-1,ease:'none'});gsap.to(group.scale,{x:1.08,y:1.08,z:1.08,duration:2.8,repeat:-1,yoyo:true,ease:'sine.inOut'});let t=0;function animate(){t+=.006;group.rotation.x=Math.sin(t*.7)*.12;renderer.setSize(canvas.clientWidth,canvas.clientHeight,false);camera.aspect=canvas.clientWidth/canvas.clientHeight;camera.updateProjectionMatrix();renderer.render(scene,camera);requestAnimationFrame(animate)}animate();}
